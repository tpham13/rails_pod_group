<h1> Student</h1>
<h3>
<%= @student.name %><br>
<%= @student.grade %><br>
<%= @student.email %><br>

</h3>

<%= link_to "Back", students_path %>
<%= link_to "Edit", student_path %>

<%= button_to "Delete", student_path(@student), method: :delete %>

